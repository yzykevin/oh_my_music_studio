name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: Existing tag to build and upload
        required: true
        type: string

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Ensure draft release exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}"
          TITLE="OMS ${TAG}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release ${TAG} already exists"
          else
            gh release create "$TAG" --title "$TITLE" --draft --generate-notes
          fi

  build-macos:
    needs: create-release
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build macOS app
        run: npm run build && npm run build:all && npx electron-builder --publish never

      - name: Upload macOS update artifacts to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}"
          for FILE in release/*.dmg release/*.zip release/latest-mac.yml; do
            [ -f "$FILE" ] && gh release upload "$TAG" "$FILE" --clobber
          done
          test -n "$(ls release/*.dmg 2>/dev/null)"
          test -n "$(ls release/*.zip 2>/dev/null)"
          test -f release/latest-mac.yml

  build-windows:
    needs: create-release
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Windows app
        run: npm run build && npm run build:all && npx electron-builder --publish never

      - name: Upload EXE to release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}"
          cd release
          for f in *.exe; do
            [ -f "$f" ] && gh release upload "$TAG" "$f" --clobber && echo "Uploaded $f"
          done
          if [ -f latest.yml ]; then
            gh release upload "$TAG" latest.yml --clobber
          fi
          if ! ls *.exe 1>/dev/null 2>&1; then
            echo "No EXE found, listing all release files:"
            ls -la
            exit 1
          fi
          test -f latest.yml

  publish-release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}"
          gh release edit "$TAG" --repo "${{ github.repository }}" --draft=false --latest
